# |---------------------------------------------------------------------------------|
# | sigmoid:																		|
# | 特点: 将任意的样本值映射集中在 0 - 1 之间的分布, 能更好的完成分类任务;					|
# | 公式: sigmoid( x ) = y = 1 ⁄ ( 1 + e⁻ˣ );										|
# |---------------------------------------------------------------------------------|

# |---------------------------------------------------------------------------------|
# | 向量内积:																			|
# | a向量 = [a₀, a₁, a₂, a₃, ... , aₙ ];												|
# | b向量 = [b₀, b₁, b₂, b₃, ... , bₙ ];												|
# | a 和 b 的内积: a₀ * b₀ + a₁ * b₁ + a₂ * b₂ + a₃ * b₃ + ... + aₙ * bₙ				|
# |---------------------------------------------------------------------------------|

# |---------------------------------------------------------------------------------|
# | 逻辑回归 logistic:																|
# | p( x ) = 1 ⁄ ( 1 + e⁻ᶿᵀˣ );														|
# |																					|
# | 决策边界 decision boundary:														|
# | hᶿ( x ) = g( θᵀx ) = θᵀx = θ₀ + θ₁ * x₁ + θ₂ * x₂ + ... + θₙ * xₙ;( 向量内积 )		|
# | θ = ( θ₀, θ₁, θ₂, ... , θₙ ); x = ( x₀, x₁, x₂, ... , xₙ ); 其中 x₀ = 1;			|
# | 1): 例如 g( x ) = θ₀ + θ₁ * x₁ + θ₂ * x₂											|
# |     g( x ) = -4 + x₁ + x₂ -> 过( 0, 4 ), ( 4, 0 ) 的线性函数						|
# | 2): 例如 g( x ) = θ₀ + θ₁ * x₁ + θ₂ * x₂ + θ₃ * x₁² + θ₄ * x₂²;					|
# |     g( x ) = -1 + x₁² + x₂² 半径为 1, 圆心 ( 0, 0 ) 的曲线函数						|
# | if y >= 0.5: y = 1;																|
# | if y < 1: y = 0;																|
# |---------------------------------------------------------------------------------|
